function sort(data)
	for i from 0 to data.length - 1
		min <- data[i]
		for j from i + 1 to data.length - 1
			if data[j] < min then
				min <- data[j]
			end if
		end for
	
		data[i] <- min
	end for
	
	return data
end function

function main()
  x <- [1,2,3,4]
  y <- 20
  node <- { value=3, left=null , right=null }
  z <- node.value
  findNode(node,20)
  return(x)
  print(x)
end function